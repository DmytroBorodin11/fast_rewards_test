import React from 'react'

function Form(props) {
    return (
        <form className="login__form">
            <h2 className="form__title">
                LOGIN
            </h2>
        </form>
    )
}

export default Form;
